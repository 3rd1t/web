library(httr)
library(jsonlite)

url <- modify_url("https://api.perfol.io/d/beta/6factor/all/europe/eur/monthly",
                  query = list(from="2009-12-31", to="2018-12-31"))

resp <- GET(url)

cont <- fromJSON(content(resp,
                         as="text",
                         encoding="UTF-8"))

factors <- data.frame(cont)

# Start analyzing!